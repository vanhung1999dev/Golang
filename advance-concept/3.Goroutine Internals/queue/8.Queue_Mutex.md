# CASE 5 — Mutex Deep Dive
### Code
```go
mu.Lock()
```

Already locked.

### Runtime Uses Semaphores

sync.Mutex eventually interacts with runtime semaphore system.

Waiting goroutines tracked in structures like:

```
semaRoot
```
(hash buckets / treap / lists depending version)

## Steps
### 1. Fast path CAS fails

Lock already owned.

### 2. Enter slow path

Current G added to mutex waiter set.

### 3. gopark()

## Unlock()
```
mu.Unlock()
```

Wakes one waiter:

```
goready(Gx)
```